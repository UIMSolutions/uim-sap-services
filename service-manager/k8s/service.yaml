apiVersion: v1
kind: Service
metadata:
  name: uim-service-manager-service
  labels:
    app: uim-service-manager-service
spec:
  selector:
    app: uim-service-manager-service
  ports:
  - name: http
    protocol: TCP
    port: 8111
    targetPort: 8111
  type: ClusterIP
