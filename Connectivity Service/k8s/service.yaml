apiVersion: v1
kind: Service
metadata:
  name: uim-sap-con
  labels:
    app: uim-sap-con
spec:
  selector:
    app: uim-sap-con
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8085
  type: ClusterIP
