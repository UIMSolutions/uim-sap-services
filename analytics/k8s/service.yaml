apiVersion: v1
kind: Service
metadata:
  name: uim-sap-analytics
  labels:
    app: uim-sap-analytics
spec:
  selector:
    app: uim-sap-analytics
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8090
  type: ClusterIP
