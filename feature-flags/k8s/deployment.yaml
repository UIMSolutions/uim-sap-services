apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-sap-ff
  labels:
    app: uim-sap-ff
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-sap-ff
  template:
    metadata:
      labels:
        app: uim-sap-ff
    spec:
      containers:
      - name: uim-sap-ff
        image: ghcr.io/uimsolutions/uim-sap-ff:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8094
        envFrom:
        - configMapRef:
            name: uim-sap-ff-config
        env:
        - name: FF_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: uim-sap-ff-secret
              key: auth-token
              optional: true
        readinessProbe:
          httpGet:
            path: /api/ff/ready
            port: http
          initialDelaySeconds: 3
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /api/ff/health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            cpu: 150m
            memory: 192Mi
          limits:
            cpu: 1000m
            memory: 1Gi
