apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-sap-cps
  labels:
    app: uim-sap-cps
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-sap-cps
  template:
    metadata:
      labels:
        app: uim-sap-cps
    spec:
      containers:
      - name: uim-sap-cps
        image: ghcr.io/uimsolutions/uim-sap-cps:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8089
        envFrom:
        - configMapRef:
            name: uim-sap-cps-config
        env:
        - name: CPS_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: uim-sap-cps-secret
              key: auth-token
              optional: true
        readinessProbe:
          httpGet:
            path: /api/cps/ready
            port: http
          initialDelaySeconds: 3
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /api/cps/health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
