apiVersion: v1
kind: Service
metadata:
  name: uim-sap-buh
  labels:
    app: uim-sap-buh
spec:
  selector:
    app: uim-sap-buh
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8083
  type: ClusterIP
