apiVersion: v1
kind: Service
metadata:
  name: uim-sap-task-center
  labels:
    app: uim-sap-task-center
spec:
  selector:
    app: uim-sap-task-center
  ports:
  - name: http
    port: 8096
    targetPort: 8096
  type: ClusterIP
