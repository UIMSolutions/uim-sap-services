apiVersion: v1
kind: Service
metadata:
  name: uim-sap-obs
  labels:
    app: uim-sap-obs
spec:
  selector:
    app: uim-sap-obs
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8091
  type: ClusterIP
