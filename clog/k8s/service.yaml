apiVersion: v1
kind: Service
metadata:
  name: uim-sap-scl
  labels:
    app: uim-sap-scl
spec:
  selector:
    app: uim-sap-scl
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8081
  type: ClusterIP
