apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-sap-scl
  labels:
    app: uim-sap-scl
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-sap-scl
  template:
    metadata:
      labels:
        app: uim-sap-scl
    spec:
      containers:
      - name: uim-sap-scl
        image: ghcr.io/uimsolutions/uim-sap-scl:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8081
        envFrom:
        - configMapRef:
            name: uim-sap-scl-config
        env:
        - name: SCL_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: uim-sap-scl-secret
              key: auth-token
              optional: true
        readinessProbe:
          httpGet:
            path: /sap/cloud/logging/v1/ready
            port: http
          initialDelaySeconds: 3
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /sap/cloud/logging/v1/health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
