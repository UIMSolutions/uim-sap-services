apiVersion: v1
kind: Service
metadata:
  name: uim-sap-dpi
  labels:
    app: uim-sap-dpi
spec:
  selector:
    app: uim-sap-dpi
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8093
  type: ClusterIP
