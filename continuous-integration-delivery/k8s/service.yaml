apiVersion: v1
kind: Service
metadata:
  name: uim-sap-cid
  labels:
    app: uim-sap-cid
spec:
  selector:
    app: uim-sap-cid
  ports:
  - name: http
    port: 8102
    targetPort: 8102
  type: ClusterIP
