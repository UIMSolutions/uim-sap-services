apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-sap-agentry
  labels:
    app: uim-sap-agentry
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-sap-agentry
  template:
    metadata:
      labels:
        app: uim-sap-agentry
    spec:
      containers:
      - name: uim-sap-agentry
        image: ghcr.io/uimsolutions/uim-sap-agentry:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8089
        envFrom:
        - configMapRef:
            name: uim-sap-agentry-config
        env:
        - name: AGENTRY_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: uim-sap-agentry-secret
              key: auth-token
              optional: true
        readinessProbe:
          httpGet:
            path: /api/agentry/ready
            port: http
          initialDelaySeconds: 3
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /api/agentry/health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            cpu: 150m
            memory: 192Mi
          limits:
            cpu: 1000m
            memory: 1Gi
