apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-sap-em
  labels:
    app: uim-sap-em
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-sap-em
  template:
    metadata:
      labels:
        app: uim-sap-em
    spec:
      containers:
      - name: uim-sap-em
        image: ghcr.io/uimsolutions/uim-sap-em:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8092
        envFrom:
        - configMapRef:
            name: uim-sap-em-config
        env:
        - name: EVM_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: uim-sap-em-secret
              key: auth-token
              optional: true
        readinessProbe:
          httpGet:
            path: /api/em/ready
            port: http
          initialDelaySeconds: 3
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /api/em/health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            cpu: 150m
            memory: 192Mi
          limits:
            cpu: 1000m
            memory: 1Gi
