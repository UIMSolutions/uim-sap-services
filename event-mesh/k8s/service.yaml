apiVersion: v1
kind: Service
metadata:
  name: uim-sap-em
  labels:
    app: uim-sap-em
spec:
  selector:
    app: uim-sap-em
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8092
  type: ClusterIP
