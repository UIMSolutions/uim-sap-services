apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-sap-mon
  labels:
    app: uim-sap-mon
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-sap-mon
  template:
    metadata:
      labels:
        app: uim-sap-mon
    spec:
      containers:
      - name: uim-sap-mon
        image: ghcr.io/uimsolutions/uim-sap-mon:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8090
        envFrom:
        - configMapRef:
            name: uim-sap-mon-config
        env:
        - name: MON_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: uim-sap-mon-secret
              key: auth-token
              optional: true
        readinessProbe:
          httpGet:
            path: /api/mon/ready
            port: http
          initialDelaySeconds: 3
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /api/mon/health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
