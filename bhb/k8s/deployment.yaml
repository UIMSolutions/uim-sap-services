apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-sap-buh
  labels:
    app: uim-sap-buh
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-sap-buh
  template:
    metadata:
      labels:
        app: uim-sap-buh
    spec:
      containers:
      - name: uim-sap-buh
        image: ghcr.io/uimsolutions/uim-sap-buh:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8083
        envFrom:
        - configMapRef:
            name: uim-sap-buh-config
        env:
        - name: BUH_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: uim-sap-buh-secret
              key: auth-token
              optional: true
        readinessProbe:
          httpGet:
            path: /api/hub/ready
            port: http
          initialDelaySeconds: 3
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /api/hub/health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
