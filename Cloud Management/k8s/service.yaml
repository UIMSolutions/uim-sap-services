apiVersion: v1
kind: Service
metadata:
  name: uim-sap-mgt
  labels:
    app: uim-sap-mgt
spec:
  selector:
    app: uim-sap-mgt
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8088
  type: ClusterIP
