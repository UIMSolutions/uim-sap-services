apiVersion: v1
kind: Service
metadata:
  name: uim-sap-cmg
  labels:
    app: uim-sap-cmg
spec:
  selector:
    app: uim-sap-cmg
  ports:
  - name: http
    port: 8095
    targetPort: 8095
  type: ClusterIP
