apiVersion: v1
kind: Service
metadata:
  name: uim-sap-html5-repo
  labels:
    app: uim-sap-html5-repo
spec:
  selector:
    app: uim-sap-html5-repo
  ports:
  - name: http
    port: 80
    targetPort: http
  type: ClusterIP
