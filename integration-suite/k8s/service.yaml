apiVersion: v1
kind: Service
metadata:
  name: uim-sap-is
  labels:
    app: uim-sap-is
spec:
  selector:
    app: uim-sap-is
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8100
  type: ClusterIP
