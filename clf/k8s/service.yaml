apiVersion: v1
kind: Service
metadata:
  name: uim-sap-clf
  labels:
    app: uim-sap-clf
spec:
  selector:
    app: uim-sap-clf
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8082
  type: ClusterIP
