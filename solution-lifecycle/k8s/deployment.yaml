apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-sap-slm
  labels:
    app: uim-sap-slm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-sap-slm
  template:
    metadata:
      labels:
        app: uim-sap-slm
    spec:
      containers:
      - name: uim-sap-slm
        image: ghcr.io/uimsolutions/uim-sap-slm:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8120
        envFrom:
        - configMapRef:
            name: uim-sap-slm-config
        env:
        - name: SLM_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: uim-sap-slm-secret
              key: auth-token
              optional: true
        readinessProbe:
          httpGet:
            path: /api/solution-lifecycle/ready
            port: http
          initialDelaySeconds: 3
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /api/solution-lifecycle/health
            port: http
          initialDelaySeconds: 8
          periodSeconds: 10
        resources:
          requests:
            cpu: 150m
            memory: 192Mi
          limits:
            cpu: 1000m
            memory: 1Gi
