apiVersion: v1
kind: Service
metadata:
  name: uim-sap-kym
  labels:
    app: uim-sap-kym
spec:
  selector:
    app: uim-sap-kym
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8088
  type: ClusterIP
