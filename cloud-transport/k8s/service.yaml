apiVersion: v1
kind: Service
metadata:
  name: uim-sap-ctm
  labels:
    app: uim-sap-ctm
spec:
  selector:
    app: uim-sap-ctm
  ports:
  - name: http
    port: 8100
    targetPort: 8100
  type: ClusterIP
