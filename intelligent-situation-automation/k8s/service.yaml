apiVersion: v1
kind: Service
metadata:
  name: uim-sap-isa
  labels:
    app: uim-sap-isa
spec:
  selector:
    app: uim-sap-isa
  ports:
  - name: http
    port: 80
    targetPort: 8088
  type: ClusterIP
