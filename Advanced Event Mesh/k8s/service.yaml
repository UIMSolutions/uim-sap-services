apiVersion: v1
kind: Service
metadata:
  name: uim-sap-aem
  labels:
    app: uim-sap-aem
spec:
  selector:
    app: uim-sap-aem
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8088
  type: ClusterIP
