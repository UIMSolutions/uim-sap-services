apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-sap-aem
  labels:
    app: uim-sap-aem
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-sap-aem
  template:
    metadata:
      labels:
        app: uim-sap-aem
    spec:
      containers:
      - name: uim-sap-aem
        image: ghcr.io/uimsolutions/uim-sap-aem:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8088
        envFrom:
        - configMapRef:
            name: uim-sap-aem-config
        env:
        - name: AEM_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: uim-sap-aem-secret
              key: auth-token
              optional: true
        readinessProbe:
          httpGet:
            path: /api/aem/ready
            port: http
          initialDelaySeconds: 3
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /api/aem/health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            cpu: 150m
            memory: 192Mi
          limits:
            cpu: 1000m
            memory: 1Gi
