apiVersion: v1
kind: Service
metadata:
  name: uim-sap-kst
  labels:
    app: uim-sap-kst
spec:
  selector:
    app: uim-sap-kst
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8087
  type: ClusterIP
