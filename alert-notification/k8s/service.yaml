apiVersion: v1
kind: Service
metadata:
  name: uim-alert-notification-service
  labels:
    app: uim-alert-notification-service
spec:
  selector:
    app: uim-alert-notification-service
  ports:
  - name: http
    protocol: TCP
    port: 8097
    targetPort: 8097
  type: ClusterIP
