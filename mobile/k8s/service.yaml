apiVersion: v1
kind: Service
metadata:
  name: uim-sap-mob
  labels:
    app: uim-sap-mob
spec:
  selector:
    app: uim-sap-mob
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8089
  type: ClusterIP
