apiVersion: v1
kind: Service
metadata:
  name: uim-sap-atm
  labels:
    app: uim-sap-atm
spec:
  selector:
    app: uim-sap-atm
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8088
  type: ClusterIP
